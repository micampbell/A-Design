; ZHAOHONG, WZH75, SUM. 2001(DEFUN HEADER()    (PRINT "ZHAOHONG WU")    (PRINT "639723193")    (PRINT "AI PROBLEM #11")    (PRINT "PRETTY-PRINT")    (PRINT "M.E.N391R")    (PRINT "SUMMER, 2001")    (TERPRI))(defun explode (object)     (loop for char across (prin1-to-string object)           collect (intern (string char))))(defun implode (list)     (read-from-string (coerce (mapcar #'character list) 'string)))(DEFUN PROCESS (YOU-SAY)    (SETQ SAY YOU-SAY)    (SETQ ELEMENT (CAR YOU-SAY))    (SETQ LEN (LENGTH SAY))    (SETQ NUM 0)    (DO       (( K 0 (+ K 1) ))      ((= K LEN) NIL)      (COND ((EQUAL ELEMENT '\() (SETQ NUM (+ NUM 1)) )            ((EQUAL ELEMENT '\)) (SETQ NUM (- NUM 1)) )            (T   (INDENT NUM) (PRINC ELEMENT) (TERPRI) )            )      (SETQ SAY (CDR SAY))      (SETQ ELEMENT (CAR SAY))    ))     (DEFUN INDENT (NUM)    (DO       (( K 1 (+ K 1) ))      ((= K NUM) NIL)      (PRINC "    ")    )  )       (DEFUN PRETTY-PRINT(LIST);     (HEADER);    (DO ;      (( J 0 (+ J 1) ));      (NIL)      (TERPRI)      (SETQ THELIST (EXPLODE LIST));      (PRINT THELIST);      (PRINT (LENGTH THELIST))      (PROCESS THELIST)      (TERPRI);    ))